# 本地实时字幕

## 首次使用

1. 打开 Chrome，在地址栏输入 `chrome://extensions`。
2. 打开右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择本目录下的 `extension` 文件夹。
4. 双击 `启动-流畅模式.cmd`。
5. 模型启动完成后播放视频。扩展图标中的状态点变绿即表示连接成功。

## 日常使用

- 普通视频：双击 `启动-流畅模式.cmd`。
- 嘈杂音频、长句或专业课程：双击 `启动-质量模式.cmd`。
- 关闭服务：双击 `停止字幕.cmd`。
- 在 Chrome 扩展弹窗中可选择源语言、字幕字号、位置以及是否显示原文。

已启动一种模式时，直接启动另一种模式即可完成切换，无需先手动停止。

## 注意事项

- Chrome 必须通过当前 Windows 默认播放设备输出声音。
- 更换耳机或音箱后，如果没有字幕，停止后重新启动即可。
- 笔记本建议接通电源，并使用 NVIDIA 高性能模式。
- 日志位于 `logs` 文件夹；模型和缓存全部保存在本目录，不占用 C 盘模型缓存。
